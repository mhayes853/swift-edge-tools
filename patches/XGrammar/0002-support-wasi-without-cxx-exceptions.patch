diff --git i/cpp/grammar_parser.cc w/cpp/grammar_parser.cc
index 1d00ec4..b848290 100644
--- i/cpp/grammar_parser.cc
+++ w/cpp/grammar_parser.cc
@@ -8,8 +8,10 @@
 #include <picojson.h>
 
 #include <algorithm>
+#include <cerrno>
 #include <cmath>
 #include <cstdint>
+#include <cstdlib>
 #include <cstring>
 #include <limits>
 #include <string>
@@ -252,13 +254,13 @@ EBNFLexer::Token EBNFLexer::Impl::ParseIdentifierOrBooleanToken() {
       if (text.empty()) {
         return false;
       }
-      try {
-        *value = std::stod(text);
-      } catch (const std::out_of_range&) {
-        *value = std::numeric_limits<double>::infinity();
-      } catch (const std::exception&) {
+      char* parsed_end = nullptr;
+      errno = 0;
+      const double parsed = std::strtod(text.c_str(), &parsed_end);
+      if (parsed_end == text.c_str()) {
         return false;
       }
+      *value = errno == ERANGE ? std::numeric_limits<double>::infinity() : parsed;
       return true;
     };
     // Parse a comma-separated attribute list. Each attribute may appear at most once.
diff --git i/cpp/lark_converter.cc w/cpp/lark_converter.cc
index e4408d4..67b7c65 100644
--- i/cpp/lark_converter.cc
+++ w/cpp/lark_converter.cc
@@ -9,7 +9,10 @@
 #include <xgrammar/exception.h>
 
 #include <algorithm>
+#include <charconv>
 #include <cctype>
+#include <cerrno>
+#include <cstdlib>
 #include <cmath>
 #include <cstdint>
 #include <limits>
@@ -39,7 +42,10 @@ struct Location {
   int column = 1;
 };
 
-[[noreturn]] void RaiseLarkError(
+#if !defined(__wasi__)
+[[noreturn]]
+#endif
+void RaiseLarkError(
     const std::string& source, const Location& location, const std::string& message
 ) {
   size_t line_start = 0;
@@ -63,7 +69,19 @@ struct Location {
   if (!line_text.empty()) {
     os << "\n" << line_text << "\n" << std::string(std::max(0, location.column - 1), ' ') << "^";
   }
+#if defined(__wasi__)
+  SetLastError(ErrorCode::kGrammar, os.str().c_str());
+#else
   throw XGrammarError(os.str());
+#endif
+}
+
+void RaiseLarkConfigurationError(const std::string& message) {
+#if defined(__wasi__)
+  SetLastError(ErrorCode::kGrammar, message.c_str());
+#else
+  throw XGrammarError(message);
+#endif
 }
 
 enum class TokenType {
@@ -364,6 +382,7 @@ class LarkLexer {
       Advance();
     }
     RaiseLarkError(source_, location, "unterminated string literal");
+    return {};
   }
 
   Token LexRegex() {
@@ -395,6 +414,7 @@ class LarkLexer {
       Advance();
     }
     RaiseLarkError(source_, location, "unterminated regular expression");
+    return {};
   }
 
   Token LexSpecialToken() {
@@ -680,6 +700,7 @@ class LarkParser {
         "expected string literal, regular expression, or uppercase terminal name after " +
             attribute_name + "="
     );
+    return {};
   }
 
   void ParseImport(Document* document) {
@@ -858,21 +879,16 @@ class LarkParser {
               source_, key.location, "temperature attribute is specified more than once"
           );
         }
-        try {
-          size_t parsed_length = 0;
-          float temperature = std::stof(value.text, &parsed_length);
-          if (parsed_length != value.text.size() || !std::isfinite(temperature) ||
-              temperature < 0) {
-            RaiseLarkError(
-                source_, value.location, "temperature must be a finite non-negative number"
-            );
-          }
-          definition->temperature = temperature;
-        } catch (const std::exception&) {
+        char* parsed_end = nullptr;
+        errno = 0;
+        float temperature = std::strtof(value.text.c_str(), &parsed_end);
+        if (errno == ERANGE || parsed_end != value.text.c_str() + value.text.size() ||
+            !std::isfinite(temperature) || temperature < 0) {
           RaiseLarkError(
               source_, value.location, "temperature must be a finite non-negative number"
           );
         }
+        definition->temperature = temperature;
       } else {
         RaiseLarkError(
             source_,
@@ -957,16 +973,17 @@ class LarkParser {
 
   int32_t ParseInteger() {
     Token token = Consume(TokenType::kNumber, "expected integer");
-    try {
-      size_t parsed = 0;
-      long long value = std::stoll(token.text, &parsed);
-      if (parsed != token.text.size() || value < 0 || value > std::numeric_limits<int32_t>::max()) {
-        RaiseLarkError(source_, token.location, "invalid non-negative repetition count");
-      }
-      return static_cast<int32_t>(value);
-    } catch (const std::exception&) {
+    int64_t value = 0;
+    const auto conversion = std::from_chars(
+        token.text.data(), token.text.data() + token.text.size(), value
+    );
+    if (conversion.ec != std::errc{} || conversion.ptr != token.text.data() + token.text.size()) {
       RaiseLarkError(source_, token.location, "invalid repetition count");
     }
+    if (value < 0 || value > std::numeric_limits<int32_t>::max()) {
+      RaiseLarkError(source_, token.location, "invalid non-negative repetition count");
+    }
+    return static_cast<int32_t>(value);
   }
 
   Node ParseExpr() {
@@ -1141,6 +1158,7 @@ class LarkParser {
       RaiseLarkError(source_, token.location, "directive " + token.text + " is not supported");
     }
     RaiseLarkError(source_, token.location, "expected grammar expression");
+    return {};
   }
 
   Node ParseStringNode(const Token& token) {
@@ -1471,6 +1489,9 @@ class LarkCompiler {
         body->kind == Node::Kind::kGrammarRef) {
       return CompileNode(*body, definition.name, false, false);
     }
+#if defined(__wasi__)
+    return CompileNode(definition.body, definition.name, true, false);
+#else
     try {
       return CompileNode(definition.body, definition.name, true, false);
     } catch (const std::exception& error) {
@@ -1480,6 +1501,7 @@ class LarkCompiler {
           std::string(error.what()) + "; temperature is only supported on terminals and subgrammars"
       );
     }
+#endif
   }
 
   void ExpandImports() {
@@ -1862,6 +1884,7 @@ class LarkCompiler {
         );
     }
     RaiseLarkError(source_, node.location, "unsupported terminal node");
+    return {};
   }
 
   std::vector<std::string> ParseStructuredRegexChunks(const Node& node) const {
@@ -1971,6 +1994,16 @@ class LarkCompiler {
     }
 
     named_grammars_.active.push_back(name);
+#if defined(__wasi__)
+    const std::string& named_source = std::get<std::string>(input_it->second);
+    auto tokens = LarkLexer(named_source).Tokenize();
+    auto document = LarkParser(named_source, std::move(tokens)).Parse();
+    Grammar compiled =
+        LarkCompiler(named_source, std::move(document), tokenizer_info_, named_grammars_).Compile();
+    auto inserted_it = named_grammars_.compiled.emplace(name, std::move(compiled)).first;
+    named_grammars_.active.pop_back();
+    return inserted_it->second;
+#else
     try {
       const std::string& named_source = std::get<std::string>(input_it->second);
       auto tokens = LarkLexer(named_source).Tokenize();
@@ -1989,6 +2022,7 @@ class LarkCompiler {
           "failed to compile named grammar '@" + name + "': " + std::string(error.what())
       );
     }
+#endif
   }
 
   int32_t CompileNode(
@@ -2077,6 +2111,11 @@ class LarkCompiler {
           int32_t result = builder_.AddRuleRef(regex_rule_id);
           return terminal_mode || !append_skip ? result : AppendSkip(result);
         }
+#if defined(__wasi__)
+        int32_t root = SubGrammarAdder::Apply(&builder_, Grammar::FromRegex(pattern));
+        int32_t result = builder_.AddRuleRef(root);
+        return terminal_mode || !append_skip ? result : AppendSkip(result);
+#else
         try {
           int32_t root = SubGrammarAdder::Apply(&builder_, Grammar::FromRegex(pattern));
           int32_t result = builder_.AddRuleRef(root);
@@ -2088,11 +2127,17 @@ class LarkCompiler {
               "failed to compile regular expression: " + std::string(error.what())
           );
         }
+#endif
       }
       case Node::Kind::kJson: {
         if (terminal_mode) {
           RaiseLarkError(source_, node.location, "%json cannot be used in terminals");
         }
+#if defined(__wasi__)
+        int32_t root = SubGrammarAdder::Apply(&builder_, Grammar::FromJSONSchema(node.text));
+        int32_t result = builder_.AddRuleRef(root);
+        return terminal_mode || !append_skip ? result : AppendSkip(result);
+#else
         try {
           int32_t root = SubGrammarAdder::Apply(&builder_, Grammar::FromJSONSchema(node.text));
           int32_t result = builder_.AddRuleRef(root);
@@ -2104,11 +2149,18 @@ class LarkCompiler {
               "failed to compile inline JSON schema: " + std::string(error.what())
           );
         }
+#endif
       }
       case Node::Kind::kNestedLark: {
         if (terminal_mode) {
           RaiseLarkError(source_, node.location, "nested %lark cannot be used in terminals");
         }
+#if defined(__wasi__)
+        LarkCompiler compiler(source_, *node.nested, tokenizer_info_, named_grammars_);
+        int32_t root = SubGrammarAdder::Apply(&builder_, compiler.Compile());
+        int32_t result = builder_.AddRuleRef(root);
+        return terminal_mode || !append_skip ? result : AppendSkip(result);
+#else
         try {
           LarkCompiler compiler(source_, *node.nested, tokenizer_info_, named_grammars_);
           int32_t root = SubGrammarAdder::Apply(&builder_, compiler.Compile());
@@ -2121,6 +2173,7 @@ class LarkCompiler {
               "failed to compile nested Lark grammar: " + std::string(error.what())
           );
         }
+#endif
       }
       case Node::Kind::kSpecialToken: {
         if (terminal_mode) {
@@ -2155,6 +2208,7 @@ class LarkCompiler {
         );
     }
     RaiseLarkError(source_, node.location, "unsupported grammar node");
+    return {};
   }
 
   int32_t AppendSkip(int32_t expression) {
@@ -2214,25 +2268,29 @@ class LarkCompiler {
                 source_, location, "invalid numeric special-token range '" + range + "'"
             );
           }
-          int64_t first;
-          int64_t last;
-          try {
-            auto parse_token_id = [](const std::string& value) {
-              std::string trimmed = Trim(value);
-              size_t parsed = 0;
-              int64_t result = std::stoll(trimmed, &parsed);
-              if (parsed != trimmed.size()) {
-                throw std::invalid_argument("trailing characters");
-              }
-              return result;
-            };
-            first = parse_token_id(range.substr(0, dash));
-            last = dash == std::string::npos ? first : parse_token_id(range.substr(dash + 1));
-          } catch (const std::exception&) {
+          auto parse_token_id = [](const std::string& value) -> std::optional<int64_t> {
+            const std::string trimmed = Trim(value);
+            int64_t result = 0;
+            const auto conversion = std::from_chars(
+                trimmed.data(), trimmed.data() + trimmed.size(), result
+            );
+            if (conversion.ec != std::errc{} ||
+                conversion.ptr != trimmed.data() + trimmed.size()) {
+              return std::nullopt;
+            }
+            return result;
+          };
+          const auto first_result = parse_token_id(range.substr(0, dash));
+          const auto last_result = dash == std::string::npos
+              ? first_result
+              : parse_token_id(range.substr(dash + 1));
+          if (!first_result.has_value() || !last_result.has_value()) {
             RaiseLarkError(
                 source_, location, "invalid numeric special-token range '" + range + "'"
             );
           }
+          const int64_t first = *first_result;
+          const int64_t last = *last_result;
           if (first < 0 || last < first || last > std::numeric_limits<int32_t>::max()) {
             RaiseLarkError(
                 source_, location, "invalid numeric special-token range '" + range + "'"
@@ -2691,18 +2749,18 @@ Grammar LarkToGrammar(
   NamedGrammarRegistry named_grammar_registry;
   for (const auto& [name, grammar_or_source] : named_grammars) {
     if (name.empty()) {
-      throw XGrammarError("Named grammar names must not be empty");
+      RaiseLarkConfigurationError("Named grammar names must not be empty");
     }
     if (!std::all_of(name.begin(), name.end(), [](unsigned char character) {
           return std::isalnum(character) || character == '_' || character == '-';
         })) {
-      throw XGrammarError(
+      RaiseLarkConfigurationError(
           "Invalid named grammar name '" + name +
           "': names may contain only letters, digits, underscores, and hyphens"
       );
     }
     if (!named_grammar_registry.inputs.emplace(name, grammar_or_source).second) {
-      throw XGrammarError("Duplicate named grammar '" + name + "'");
+      RaiseLarkConfigurationError("Duplicate named grammar '" + name + "'");
     }
   }
   auto tokens = LarkLexer(lark_string).Tokenize();
diff --git i/cpp/support/json_parse.h w/cpp/support/json_parse.h
index 3deba69..f9b78fb 100644
--- i/cpp/support/json_parse.h
+++ w/cpp/support/json_parse.h
@@ -94,12 +94,16 @@ template <typename Iter>
 inline Iter ParseJSON(picojson::value& out, Iter begin, Iter end, std::string* err) {
   int depth = 0;
   detail::DepthGuardedParseContext ctx(&out, &depth);
+#if defined(__wasi__)
+  return picojson::_parse(ctx, begin, end, err);
+#else
   try {
     return picojson::_parse(ctx, begin, end, err);
   } catch (const LogFatalError& error) {
     *err = error.what();
     return begin;
   }
+#endif
 }
 
 /*!
diff --git i/cpp/support/no_exceptions.h w/cpp/support/no_exceptions.h
new file mode 100644
index 0000000..1879b82
--- /dev/null
+++ w/cpp/support/no_exceptions.h
@@ -0,0 +1,37 @@
+/*!
+ * Copyright (c) 2026 by Contributors
+ * \file xgrammar/support/no_exceptions.h
+ * \brief Exception-runtime compatibility configuration.
+ */
+#ifndef XGRAMMAR_SUPPORT_NO_EXCEPTIONS_H_
+#define XGRAMMAR_SUPPORT_NO_EXCEPTIONS_H_
+
+#if defined(XGRAMMAR_CXX_EXCEPTIONS_ENABLED) && !XGRAMMAR_CXX_EXCEPTIONS_ENABLED
+#ifdef __cpp_exceptions
+#pragma clang diagnostic push
+#pragma clang diagnostic ignored "-Wbuiltin-macro-redefined"
+#undef __cpp_exceptions
+#pragma clang diagnostic pop
+#endif
+#ifndef PICOJSON_DISABLE_EXCEPTION
+#define PICOJSON_DISABLE_EXCEPTION 1
+#endif
+#endif
+
+#include <cstdint>
+
+namespace xgrammar {
+
+enum class ErrorCode : uint8_t {
+  kNone,
+  kGrammar,
+};
+
+void ClearLastError();
+void SetLastError(ErrorCode code, const char* message);
+bool HasLastError();
+const char* LastErrorMessage();
+
+}  // namespace xgrammar
+
+#endif  // XGRAMMAR_SUPPORT_NO_EXCEPTIONS_H_
diff --git i/cpp/support/thread_pool.h w/cpp/support/thread_pool.h
index fcca4b9..cea12fc 100644
--- i/cpp/support/thread_pool.h
+++ w/cpp/support/thread_pool.h
@@ -60,6 +60,9 @@ class ThreadPool {
             task = std::move(task_queue_.front());
             task_queue_.pop();
           }
+#if defined(__wasi__)
+          task();
+#else
           try {
             task();
           } catch (...) {
@@ -68,6 +71,7 @@ class ThreadPool {
               first_exception_ = std::current_exception();
             }
           }
+#endif
           TaskComplete();
         }
       });
@@ -180,6 +184,7 @@ class ThreadPool {
   }
 
   void RethrowTaskException() {
+#if !defined(__wasi__)
     std::exception_ptr exception;
     {
       std::unique_lock<std::mutex> lock(queue_mutex_);
@@ -188,6 +193,7 @@ class ThreadPool {
     if (exception) {
       std::rethrow_exception(exception);
     }
+#endif
   }
 
   void TaskComplete() {
@@ -212,8 +218,10 @@ class ThreadPool {
   bool shutdown_ = false;
   /*! \brief Number of unfinished tasks */
   int unfinished_task_count_ = 0;
+#if !defined(__wasi__)
   /*! \brief The first exception thrown by a task, rethrown by Wait() or Join() */
   std::exception_ptr first_exception_ = nullptr;
+#endif
 };
 
 inline void ParallelFor(int low, int high, int num_threads, std::function<void(int)> f) {
