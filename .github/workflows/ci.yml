---
name: CI

"on":
  push:
    branches: ["main"]
  pull_request:
    branches: ["main"]

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  # macos:
  #   name: macOS Tests
  #   runs-on: macos-26
  #   timeout-minutes: 20
  #   steps:
  #     - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262
  #       with:
  #         persist-credentials: false
  #         submodules: recursive
  #     - name: Select Xcode 26.4
  #       run: sudo xcode-select -s /Applications/Xcode_26.4.app
  #     - name: Set up Python 3.12
  #       uses: actions/setup-python@a26af69be951a213d495a4c3e4e4022e16d87065
  #       with:
  #         python-version: "3.12"
  #         cache: pip
  #         cache-dependency-path: python/pyproject.toml
  #     - name: Install Python exporter
  #       run: |
  #         python3 -m venv python/.venv
  #         python/.venv/bin/python -m pip install -e python
  #     - name: Run tests
  #       env:
  #         NEEDLE_INFERENCE_TESTS: basic
  #       run: >
  #         swift test --disable-experimental-prebuilts --enable-all-traits

  # python-macos:
  #   name: macOS Python Tests
  #   runs-on: macos-26
  #   timeout-minutes: 20
  #   steps:
  #     - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262
  #       with:
  #         persist-credentials: false
  #         submodules: recursive
  #     - name: Set up Python 3.12
  #       uses: actions/setup-python@a26af69be951a213d495a4c3e4e4022e16d87065
  #       with:
  #         python-version: "3.12"
  #         cache: pip
  #         cache-dependency-path: python/pyproject.toml
  #     - name: Install Python package
  #       run: python3 -m pip install -e .
  #       working-directory: python
  #     - name: Run Python test suite
  #       run: python3 -m unittest
  #       working-directory: python

  # linux:
  #   name: Linux Tests
  #   runs-on: ubuntu-latest
  #   timeout-minutes: 20
  #   steps:
  #     - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262
  #       with:
  #         persist-credentials: false
  #         submodules: recursive
  #     - name: Check disk space
  #       run: df -h
  #     - name: Run tests
  #       run: >
  #         scripts/test-linux.sh --all-tests --python-venv

  # android:
  #   name: Android Build
  #   runs-on: ubuntu-latest
  #   timeout-minutes: 20
  #   steps:
  #     - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262
  #       with:
  #         persist-credentials: false
  #         submodules: recursive
  #     - name: Build Swift package for Android
  #       uses: "swift-android-sdk/swift-android-action@\
  #         2044b79660f201ccef8cbce62877e4ec5409f9c8"
  #       with:
  #         swift-version: 6.3
  #         swift-build-flags: >
  #           --disable-experimental-prebuilts
  #           --disable-default-traits
  #         build-tests: false
  #         run-tests: false

  embedded:
    name: Embedded Swift Smoke Test
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262
        with:
          persist-credentials: false
          submodules: recursive
      - name: Resolve development snapshot
        id: snapshot
        run: |
          set -euo pipefail
          tag="$(scripts/smoke-embedded.sh --print-snapshot)"
          echo "tag=$tag" >> "$GITHUB_OUTPUT"
          echo "Resolved $tag."
      - name: Install Swift development snapshot
        uses: SwiftyLab/setup-swift@76e3e48ac9c85b393a30adefd96afe0395b23c29
        with:
          # The smoke script resolves the snapshot from the Wasm SDK feed so
          # the host toolchain and SDK always use the same snapshot.
          swift-version: ${{ steps.snapshot.outputs.tag }}
          # Archiving the installed development toolchain duplicates several
          # gigabytes on the runner before the WASM SDK has been installed.
          cache-snapshot: false
      - name: Cache Swift SDKs
        uses: actions/cache@0057852bfaa89a56745cba8c7296529d2fc39830
        with:
          path: ~/.config/swiftpm/swift-sdks
          key: swift-sdks-${{ runner.os }}-${{ steps.snapshot.outputs.tag }}
      - name: Cache build directories
        uses: actions/cache@0057852bfaa89a56745cba8c7296529d2fc39830
        with:
          path: swift/EmbeddedSmoke/.build
          key: >-
            spm-embedded-v2-${{ runner.os }}-${{ steps.snapshot.outputs.tag }}-${{
            hashFiles('Package.resolved',
            'swift/EmbeddedSmoke/Package.resolved') }}
          restore-keys: |
            spm-embedded-v2-${{ runner.os }}-${{ steps.snapshot.outputs.tag }}-
      - name: Run the embedded smoke test
        env:
          EMBEDDED_SWIFT_SNAPSHOT: ${{ steps.snapshot.outputs.tag }}
        run: scripts/smoke-embedded.sh

  # windows:
  #   name: Windows Build
  #   runs-on: windows-latest
  #   timeout-minutes: 20
  #   steps:
  #     - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262
  #       with:
  #         persist-credentials: false
  #         submodules: recursive
  #     - name: Set up Swift 6.3
  #       uses: "compnerd/gha-setup-swift@\
  #         eeda069c5bc95ac8a9ac5cea7d4f588ae5420ca5"
  #       with:
  #         swift-version: swift-6.3-release
  #         swift-build: 6.3-RELEASE
  #     - name: Build Swift package
  #       run: >
  #         swift build --disable-experimental-prebuilts
